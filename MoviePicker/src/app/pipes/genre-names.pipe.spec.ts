import {GenreNamesPipe} from './genre-names.pipe';

describe('GenreNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new GenreNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
